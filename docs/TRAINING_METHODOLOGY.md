# Training Methodology

**Status: not yet written.** This will document, once actually done:

- Train / validation / test split strategy (and how leakage is avoided)
- Normalization approach (fit on training data only)
- Loss function and optimizer
- Number of epochs, batch size, and how they were chosen
- Checkpointing strategy

To be filled in as each piece is implemented — not written in advance of
the real training run.
